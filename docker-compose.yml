services:
  nginx:
    image: nginx:alpine
    container_name: webssh-proxy
    restart: unless-stopped
    ports:
      - "8888:8888"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - ttyd

  ttyd:
    build: .
    container_name: ttyd
    restart: unless-stopped
    expose:
      - "7681"
    volumes:
      - ./launcher.sh:/usr/local/bin/launcher.sh:ro
      - ./data:/data
    command:
      - ttyd
      - -W
      - -t
      - copyOnSelect=true
      - /usr/local/bin/launcher.sh
